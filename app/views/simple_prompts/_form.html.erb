<%= form_for(@simple_prompt) do |f| %>
  <% if @simple_prompt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@simple_prompt.errors.count, "error") %> prohibited this simple_prompt from being saved:</h2>

      <ul>
      <% @simple_prompt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
